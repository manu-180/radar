# 03 — Esquema de base de datos en Supabase

## Contexto
**Lead Detector** monitorea plataformas online buscando gente que busca contratar un developer, filtra los posts con IA (Claude) y avisa por WhatsApp. Stack: Next.js (App Router) + Supabase + Claude API + Wassenger.

Paso **03 de 35**.

**Ya existe:** proyecto Next.js, `lib/env.ts`, `lib/supabase/admin.ts`, infraestructura compartida (pasos 00–02).

Tenés el **MCP de Supabase conectado**: aplicá migraciones y corré SQL con las herramientas MCP (`apply_migration`, `execute_sql`, `list_tables`, `generate_typescript_types`).

## Tarea
1. Crear `supabase/migrations/0001_initial_schema.sql` con todo el SQL de abajo.
2. Aplicar la migración con el MCP (`apply_migration`).
3. Verificar con `list_tables` que existen las 6 tablas.
4. Generar los tipos TypeScript en `types/database.ts` (`generate_typescript_types`).

## SQL completo

```sql
-- ===== sources =====
create table sources (
  id            bigint generated always as identity primary key,
  slug          text unique not null,
  name          text not null,
  enabled       boolean not null default true,
  config        jsonb not null default '{}'::jsonb,
  cursor        jsonb not null default '{}'::jsonb,   -- high-water-mark del polling incremental
  poll_interval_minutes integer not null default 20,
  last_polled_at timestamptz,
  created_at    timestamptz not null default now()
);

-- ===== keywords (config del pre-filtro, editable sin código) =====
create table keywords (
  id         bigint generated always as identity primary key,
  term       text not null,
  kind       text not null check (kind in ('include','exclude')),
  lang       text not null default 'any' check (lang in ('any','es','en')),
  enabled    boolean not null default true,
  created_at timestamptz not null default now()
);

-- ===== leads (almacén de leads + cola de clasificación y notificación) =====
create table leads (
  id              bigint generated always as identity primary key,
  source          text not null,
  external_id     text,
  content_hash    text not null,
  title           text,
  body            text,
  url             text,
  author          text,
  lang            text,
  posted_at       timestamptz,
  raw             jsonb,
  prefilter_matched text[],
  llm_status      text not null default 'pending'
                    check (llm_status in ('pending','processing','done','failed','skipped')),
  llm_attempts    integer not null default 0,
  llm_model       text,
  llm_error       text,
  input_tokens    integer,
  output_tokens   integer,
  llm_cost_usd    numeric(10,6),
  score           integer,
  category        text check (category in ('hiring','maybe','noise')),
  reason          text,
  suggested_reply text,
  notify_status   text not null default 'pending'
                    check (notify_status in ('pending','sending','sent','failed','skipped')),
  notified_at     timestamptz,
  feedback        text check (feedback in ('responded','interested','not_relevant')),
  feedback_at     timestamptz,
  first_seen_at   timestamptz not null default now(),
  last_seen_at    timestamptz not null default now()
);
create unique index leads_source_extid_idx on leads (source, external_id) where external_id is not null;
create unique index leads_content_hash_idx on leads (content_hash);
create index leads_pending_idx on leads (first_seen_at) where llm_status = 'pending';
create index leads_notify_pending_idx on leads (first_seen_at) where notify_status = 'pending';
create index leads_first_seen_idx on leads (first_seen_at desc);

-- ===== runs (observabilidad + base de métricas y health) =====
create table runs (
  id              bigint generated always as identity primary key,
  kind            text not null check (kind in ('poll','process','notify','health')),
  source          text,
  status          text not null default 'running' check (status in ('running','ok','error')),
  items_found     integer not null default 0,
  items_new       integer not null default 0,
  items_processed integer not null default 0,
  error           text,
  started_at      timestamptz not null default now(),
  finished_at     timestamptz
);
create index runs_started_idx on runs (started_at desc);
create index runs_kind_idx on runs (kind, status, started_at desc);

-- ===== notifications (tracking de entrega WhatsApp) =====
create table notifications (
  id           bigint generated always as identity primary key,
  lead_id      bigint references leads(id) on delete cascade,
  channel      text not null default 'whatsapp',
  wassenger_id text,
  status       text not null default 'queued' check (status in ('queued','sent','delivered','failed')),
  error        text,
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index notifications_lead_idx on notifications (lead_id);

-- ===== settings (configuración key-value) =====
create table settings (
  key        text primary key,
  value      jsonb not null,
  updated_at timestamptz not null default now()
);

-- ===== RLS: cerrado al público =====
alter table sources       enable row level security;
alter table keywords      enable row level security;
alter table leads         enable row level security;
alter table runs          enable row level security;
alter table notifications enable row level security;
alter table settings      enable row level security;
-- Sin políticas: el acceso es solo server-side con el service role, que bypassa RLS.

-- ===== Datos semilla — fuentes =====
insert into sources (slug, name, enabled, config) values
  ('hackernews', 'Hacker News', true,
   '{"keywordQueries": ["developer","web app","mobile app"], "includeWhoIsHiring": true}'::jsonb),
  ('reddit', 'Reddit', true,
   '{"subreddits": ["forhire","jobbit","slavelabour","DoneDirtCheap","hiring"], "hiringFlairOnly": true}'::jsonb),
  ('bluesky', 'Bluesky', true,
   '{"queries": ["looking for a developer","need a developer","hire a developer","necesito un desarrollador","busco programador"]}'::jsonb),
  ('freelancer', 'Freelancer.com', true,
   '{"queries": ["website","web app","mobile app","next.js"]}'::jsonb),
  ('rss', 'Feeds RSS', true,
   '{"feeds": [{"name":"craigslist-sfbay","url":"https://sfbay.craigslist.org/search/cpg?format=rss"},{"name":"weworkremotely-prog","url":"https://weworkremotely.com/categories/remote-programming-jobs.rss"}]}'::jsonb),
  ('telegram', 'Telegram', false,
   '{"channels": []}'::jsonb),
  ('discord', 'Discord', false,
   '{"channels": []}'::jsonb);

-- ===== Datos semilla — keywords =====
insert into keywords (term, kind, lang) values
  ('need a developer','include','en'),
  ('looking for a developer','include','en'),
  ('hiring a developer','include','en'),
  ('looking to hire','include','en'),
  ('build a website','include','en'),
  ('build an app','include','en'),
  ('need a web developer','include','en'),
  ('need an app','include','en'),
  ('web development','include','en'),
  ('developer needed','include','en'),
  ('necesito un desarrollador','include','es'),
  ('busco desarrollador','include','es'),
  ('busco programador','include','es'),
  ('necesito una web','include','es'),
  ('necesito una pagina','include','es'),
  ('necesito una app','include','es'),
  ('desarrollador web','include','es'),
  ('quiero una pagina web','include','es'),
  ('alguien que programe','include','es'),
  ('contratar desarrollador','include','es'),
  ('looking for work','exclude','en'),
  ('seeking work','exclude','en'),
  ('my portfolio','exclude','en'),
  ('available for hire','exclude','en'),
  ('open to work','exclude','en'),
  ('busco trabajo','exclude','es'),
  ('busco empleo','exclude','es'),
  ('ofrezco mis servicios','exclude','es'),
  ('disponible para trabajar','exclude','es');

-- ===== Datos semilla — settings =====
insert into settings (key, value) values
  ('freelancer_profile', '"Developer freelance. Hace webs y apps a medida con Next.js, React y back-ends modernos. Entrega rápida y precios competitivos."'::jsonb),
  ('notify_rule', '{"categories": ["hiring"], "minScore": 70}'::jsonb),
  ('feedback_examples_count', '6'::jsonb),
  ('max_notifications_per_run', '10'::jsonb);
```

## Decisiones de diseño (no las cambies)
- **RLS activado, sin políticas:** todo el acceso es server-side con el service role.
- La tabla `leads` es a la vez el almacén, la **cola de clasificación** (`llm_status`) y la **cola de notificación** (`notify_status`).
- Deduplicación a nivel de base: índice único en `(source, external_id)` y en `content_hash`.
- `bluesky`, `freelancer` y `rss` arrancan habilitadas (no requieren setup manual). `telegram` y `discord` arrancan deshabilitadas: se activan en sus pasos de setup (30 y 32).
- `sources.cursor` guarda el high-water-mark de cada fuente para el polling incremental.

## Criterio de éxito
- `list_tables` muestra: `sources`, `keywords`, `leads`, `runs`, `notifications`, `settings`.
- Hay 7 filas en `sources`, al menos 25 en `keywords` y 4 en `settings`.
- `types/database.ts` existe y `npm run build` pasa.
